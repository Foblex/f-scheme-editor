import { AbstractControl } from '@angular/forms';

export interface IMixedValueForm {

  control1: AbstractControl<any, number>;

  control2: AbstractControl<any, number>;

  control3: AbstractControl<any, number>;

  control4: AbstractControl<any, number>;
}
