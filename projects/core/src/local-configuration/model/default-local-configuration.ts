import { ILocalConfiguration } from './i-local-configuration';

export const DEFAULT_LOCAL_CONFIGURATION: ILocalConfiguration = {

  explorerPanel: {
    isVisible: true,
  },
  editorDrawer: {
  }
}
